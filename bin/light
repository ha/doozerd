#!/bin/sh

set -e

exec 2>&1

n=$1 ; [ -z $n ] && n=1

p=`expr 8040 + $n`
w=`expr 8080 + $n`

if [ $n = "1" ]
then doozerd                   -l 127.0.0.1:$p -w :$w
else doozerd -a 127.0.0.1:8041 -l 127.0.0.1:$p -w :$w
fi
